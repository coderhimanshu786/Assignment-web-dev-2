Ans-1
/*
The declaration is not an HTML tag. All the HTML documents must start with a <!DOCTYPE> declaration because HTML <! DOCTYPE> tag is used to inform the browser about the version of HTML used in the document.
*/